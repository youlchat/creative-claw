# Phase 2.5 final review fix report

Date: 2026-07-29  
Status: Ready（原 review 的 Critical / Important 均已处理；无已知阻塞项）

Binding sources:

- `docs/superpowers/specs/2026-07-29-reference-blueprint-migration-design.md`
- `docs/superpowers/plans/2026-07-29-phase2.5-reference-blueprint-migration.md`
- `docs/superpowers/reviews/2026-07-29-phase2.5-final-review.md`

约束：项目不是 Git 仓库，未初始化、未提交；所有自动化测试均使用 deterministic agents，未调用真实模型。

## Critical fixes

### C1. Failed run reuse / retry attempt isolation

- 处理：`creative_claw/blueprint_orchestrator.py` 只复用 `completed` run；失败 run 保留审计记录，重试创建独立 attempt/idempotency；merge barrier 要求全部 required run 完成。
- RED：`test_retryable_failed_run_is_not_reused_as_completed` 在旧实现中复用了 failed run，任务错误完成。
- RED/GREEN 命令：
  `.\.venv\Scripts\python.exe -m unittest tests.test_blueprint_orchestrator.BlueprintOrchestratorTests.test_retryable_failed_run_is_not_reused_as_completed -v`
- GREEN：focused suite 40/40；full discovery 覆盖通过。

### C2. Pause/cancel tail race and atomic publication

- 处理：`creative_claw/blueprint_orchestrator.py` 在每个 agent 后、batch completion 前、merge 前重查 desired state；`creative_claw/blueprint_service.py` 的 reference/target publication 以 conditional update 原子完成。
- RED：`test_pause_requested_by_last_agent_prevents_batch_and_job_completion` 旧实现会在最后一个 agent 后越过 pause；cancel 路径会继续调用 agent。
- RED/GREEN 命令：
  `.\.venv\Scripts\python.exe -m unittest tests.test_blueprint_orchestrator.BlueprintOrchestratorTests.test_pause_requested_by_last_agent_prevents_batch_and_job_completion tests.test_blueprint_orchestrator.BlueprintOrchestratorTests.test_cancelled_job_never_calls_agent -v`
- GREEN：两条 focused 用例及完整 focused/full suite 通过。

### C3. Candidate acceptance stale/non-atomic

- 处理：`creative_claw/blueprint_service.py` 在单一 `BEGIN IMMEDIATE` 事务内校验 request expected = candidate base = current version，条件更新 `passed -> accepted`，并写 version、impact、ledger；accept/reject 和 accept/accept 竞态只允许一个终态成功。
- RED：旧实现接受 stale base，失败注入留下部分 version/candidate/ledger，竞态可重复接受。
- RED/GREEN 命令：
  `.\.venv\Scripts\python.exe -m unittest tests.test_blueprint_service.BlueprintServiceTests.test_accept_rejects_candidate_whose_base_does_not_equal_requested_and_current_version tests.test_blueprint_service.BlueprintServiceTests.test_candidate_accept_rolls_back_version_candidate_and_ledger_on_injected_failure tests.test_blueprint_service.BlueprintServiceTests.test_accept_reject_race_has_exactly_one_terminal_transition -v`
- GREEN：三条用例及 full suite 通过。

### C4. Draft firewall scanned only keys/provenance

- 处理：`DraftContextBuilder` 只序列化 target allowlist；`creative_claw/blueprint_service.py` 在真实 planner/writer 请求边界扫描 reference text、quotes、rare phrases、fingerprints 及 provenance，命中时在模型调用前阻止并写安全账本。
- RED：reference 片段藏入允许字段值时旧实现仍会发送；伪造 reference provenance 未覆盖实际 payload。
- RED/GREEN 命令：
  `.\.venv\Scripts\python.exe -m unittest tests.test_blueprint_service.BlueprintServiceTests.test_draft_context_excludes_reference_and_firewall_records_provenance_block tests.test_blueprint_service.BlueprintServiceTests.test_final_generation_request_scans_for_reference_fragments_hidden_in_allowed_values -v`
- GREEN：两条用例通过；E2E 捕获的 `/draft-candidates` 请求键仅为 `artifact_id,target_blueprint_id,unit_id`。

### C5. Candidate bypassed structural/rare-phrase/typed safety gates

- 处理：`creative_claw/blueprint_service.py` 从持久化 reference nodes/mappings/style 数据读取 ordered beats、rare phrases 和映射；`creative_claw/blueprint_similarity.py` 运行三层门禁，并消费校验后的 typed safety verdict。
- RED：旧流程以空 beats/mappings/rare phrases 评估，typed safety agent 无法升级 deterministic pass。
- RED/GREEN 命令：
  `.\.venv\Scripts\python.exe -m unittest tests.test_blueprint_service.BlueprintServiceTests.test_candidate_gate_uses_persisted_reference_beats_mappings_and_rare_phrases tests.test_blueprint_service.BlueprintServiceTests.test_typed_similarity_safety_verdict_can_escalate_a_deterministic_pass -v`
- GREEN：两条服务测试以及 5 条 similarity 测试通过。

### C6. Typed agent outputs were ignored

- 处理：`creative_claw/blueprint_agents.py` 定义 typed contracts；`creative_claw/blueprint_orchestrator.py` 合并真实 nodes/edges/interpretations/conflicts/style；`creative_claw/blueprint_service.py` 持久化真实 target-setting、mapping 与 target-blueprint 输出。
- RED：旧实现输出固定 prose/最后 batch 覆盖，typed hierarchy、interpretation/conflict、setting/mapping/target 数据未进入正式对象。
- RED/GREEN 命令：
  `.\.venv\Scripts\python.exe -m unittest tests.test_blueprint_orchestrator.BlueprintOrchestratorTests.test_typed_agent_outputs_become_hierarchy_edges_interpretations_and_conflicts tests.test_blueprint_service.BlueprintServiceTests.test_reference_publication_persists_typed_hierarchy_edges_and_agent_interpretations tests.test_blueprint_service.BlueprintServiceTests.test_setting_mapping_and_target_blueprint_consume_typed_agent_outputs -v`
- GREEN：三条用例及 E2E 的层级/冲突/解释断言通过。

## Important fixes

### I1. Publication/edit transactions

- 处理：`creative_claw/blueprint_service.py`、`creative_claw/blueprint_repository.py` 和 ledger 写入均可复用同一 connection；reference publication/edit、target publication、candidate accept 各自在单一事务中完成。
- RED：reference publication/edit 失败注入曾留下部分行；最后新增的 target mapping 失败注入得到 `target_count == 1`（期望 `0`）。
- RED/GREEN 命令：
  `.\.venv\Scripts\python.exe -m unittest tests.test_blueprint_service.BlueprintServiceTests.test_reference_publication_rolls_back_all_blueprint_rows_on_injected_failure tests.test_blueprint_service.BlueprintServiceTests.test_blueprint_edit_rolls_back_version_and_structure_on_injected_node_failure tests.test_blueprint_service.BlueprintServiceTests.test_target_blueprint_publication_rolls_back_on_injected_mapping_failure -v`
- GREEN：target rollback 单测 1/1（7.671s）；三条事务测试与 full suite 通过。

### I2. Startup recovery

- 处理：`creative_claw/db.py` 初始化 schema v5 时把 interrupted running jobs/batches 恢复为可恢复状态，不 dispatch agent。
- RED：旧启动流程保留 running 状态。
- RED/GREEN 命令：
  `.\.venv\Scripts\python.exe -m unittest tests.test_blueprint_schema.BlueprintSchemaTests.test_schema_v5_startup_recovers_interrupted_running_jobs_without_dispatch -v`
- GREEN：用例通过。

### I3. Concurrent reference idempotency

- 处理：`creative_claw/blueprint_service.py` / `creative_claw/blueprint_repository.py` 以 project/source hash/rights/prompt 生成稳定键，事务内复用 source/job；并发重复提交不创建重复对象。
- RED：双线程旧实现创建多个 source/job。
- RED/GREEN 命令：
  `.\.venv\Scripts\python.exe -m unittest tests.test_blueprint_service.BlueprintServiceTests.test_concurrent_duplicate_reference_submission_reuses_source_and_job -v`
- GREEN：用例通过。

### I4. Cross-project write references

- 处理：`creative_claw/blueprint_repository.py` 的 node/evidence/interpretation/conflict/edge/setting/mapping/candidate/similarity 写 API 在同一事务校验全部 foreign reference 的 project，跨项目统一 `KeyError`。
- RED：旧 repository 接受跨项目 foreign IDs。
- RED/GREEN 命令：
  `.\.venv\Scripts\python.exe -m unittest tests.test_blueprint_schema.BlueprintSchemaTests.test_repository_rejects_cross_project_foreign_references_on_writes -v`
- GREEN：用例通过；原有 read isolation 用例也通过。

### I5. Evidence range clamping

- 处理：`creative_claw/blueprint_orchestrator.py` 在 run completion 前验证 local batch range；越界记为 `evidence_invalid` 并阻止 merge，不再 clamp。
- RED：越界 evidence 被静默裁剪并允许完成。
- RED/GREEN 命令：
  `.\.venv\Scripts\python.exe -m unittest tests.test_blueprint_orchestrator.BlueprintOrchestratorTests.test_out_of_batch_evidence_marks_run_invalid_and_blocks_merge -v`
- GREEN：用例通过。

### I6. Public job response allowlist

- 处理：`creative_claw/api.py` 使用显式 job response allowlist，只返回 identifiers、hash/count、state/progress/timestamps 与安全错误；不回传 checkpoint 或 agent-derived text。
- RED：旧 API job JSON 暴露 checkpoint/input text。
- RED/GREEN 命令：
  `.\.venv\Scripts\python.exe -m unittest tests.test_blueprint_api.BlueprintApiTests.test_short_text_http_flow_to_accepted_candidate tests.test_blueprint_api.BlueprintApiTests.test_background_long_job_pause_resume_cancel_and_json_errors -v`
- GREEN：API 3/3 通过；蓝图 E2E report 只保存 reference hash。

### I7. Author editing and explicit confirmation

- 处理：`creative_claw/web/index.html`、`creative_claw/web/app.js`、`creative_claw/web/blueprint-state.js` 支持节点标题/摘要编辑、interpretation/conflict 裁决、结构化 target setting 编辑；setting 必须调用 confirm endpoint 后才启用 migration，target blueprint 仍需再次确认后才可生成候选。
- RED：旧 UI 无 decision controls，setting 创建后可直接迁移。
- RED/GREEN 命令：
  `node --test tests/js/blueprint-state.test.cjs`
  `.\.venv\Scripts\python.exe -m unittest tests.test_blueprint_service.BlueprintServiceTests.test_blueprint_edit_versions_author_interpretation_and_conflict_decisions tests.test_blueprint_service.BlueprintServiceTests.test_setting_structure_migration_firewall_mapping_and_confirmation -v`
- GREEN：Node blueprint state 7/7；全部 Node 16/16；蓝图 E2E 完成 setting 编辑→确认→迁移。

### I8. Stable automation error and no-model manual route

- 处理：缺少 required agent 时返回稳定 `automation_unavailable`；新增 manual reference/target blueprint service、API 和 UI 路径，仍运行 schema/version/setting confirmation 校验。
- RED：缺少 agent 的错误不稳定且无可用手工闭环。
- RED/GREEN 命令：
  `.\.venv\Scripts\python.exe -m unittest tests.test_blueprint_service.BlueprintServiceTests.test_missing_automation_has_stable_error_and_manual_blueprints_remain_versionable tests.test_blueprint_api.BlueprintApiTests.test_manual_reference_and_target_blueprint_endpoints_work_without_model_calls -v`
- GREEN：两条用例通过。

## Minor fixes

- M1：`creative_claw/blueprint_similarity.py` 改为带 candidate/reference 定位的滑动窗口表达比较，结构匹配率分母改为 reference beat 数。RED/GREEN：
  `.\.venv\Scripts\python.exe -m unittest tests.test_blueprint_similarity.BlueprintSimilarityTests.test_expression_uses_sliding_windows_and_reports_candidate_and_reference_locations tests.test_blueprint_similarity.BlueprintSimilarityTests.test_structural_match_ratio_uses_reference_denominator -v`
- M2：`tests/test_blueprint_schema.py` 的 v4→v5 fixture 从真正缺少 blueprint tables 的 v4 schema 起步。RED/GREEN：
  `.\.venv\Scripts\python.exe -m unittest tests.test_blueprint_schema.BlueprintSchemaTests.test_v4_upgrade_preserves_workflow_artifacts_ohlc_and_ledger -v`

## Follow-up hardening found during final verification

- Target migration publication：`create_mapping(..., connection=...)` 与 target artifact/version/nodes/mappings/dependencies/job completion 共享 `BEGIN IMMEDIATE`；失败注入 RED 为残留 1 个 target artifact，修复后 1/1 GREEN。
- Idempotent migration retry：`create_migration_job()` 在稳定 `create_job()` 后若已有 `output_artifact_id` 立即返回，不重复运行 mapping/target agents。
- Deterministic read order：同一毫秒批量插入的 interpretation/mapping 原先用随机 UUID 破同分，focused 首轮得到 2 个顺序失败；`creative_claw/blueprint_repository.py` 改用 SQLite insertion `rowid` 破同分。targeted GREEN 2/2（17.556s），fresh focused 40/40。
- 文档：`README.md` 与工作区 `未完成的任务.txt` 已同步显式 setting confirmation，以及 `reference-blueprints/manual`、`target-settings/{id}/confirm`、`target-blueprints/manual`。

## Fresh verification evidence

```text
py_compile blueprint_service.py + blueprint_repository.py: exit 0
Target migration rollback regression: 1/1, OK (7.671s)
Ordering regressions: 2/2, OK (17.556s)
Blueprint focused Python: 40/40, OK (218.310s)
Python compileall: exit 0
Python unittest discovery: 78 total, 77 passed, 1 real-model skipped (258.364s)
Node tests: 16/16, pass
JS syntax checks: context-state.js / blueprint-state.js / app.js, all exit 0
Phase 1 context E2E: passed=true, exit 0
Phase 2.5 blueprint E2E: passed=true, exit 0
```

Commands:

```powershell
.\.venv\Scripts\python.exe -m compileall -q creative_claw tests scripts
.\.venv\Scripts\python.exe -m unittest discover -s tests -v
node --test tests/js/*.test.cjs
node --check creative_claw/web/context-state.js
node --check creative_claw/web/blueprint-state.js
node --check creative_claw/web/app.js
.\.venv\Scripts\python.exe scripts\e2e_context_preview.py
.\.venv\Scripts\python.exe scripts\e2e_blueprint_lab.py
```

Security/privacy evidence:

- credential-shaped literal scan: `NO_CREDENTIAL_SHAPED_LITERALS`.
- blueprint E2E report reference-text/key scan: `REPORT_HASH_ONLY_AND_DRAFT_ALLOWLIST_OK`.
- browser storage scan: `NO_REFERENCE_OR_KEY_BROWSER_STORAGE`; only active project id and canvas positions use `localStorage`.
- `CREATIVE_CLAW_REAL_LLM_TEST=0`：1 paid real-model test skipped，未产生模型调用。
- E2E 的 draft requests 两次都只有 `artifact_id`、`target_blueprint_id`、`unit_id`；report 保存 reference/safe-draft SHA-256，不保存参考正文。

## Follow-up self-audit（C4/C6）

本节记录主代理在首轮修复后继续自查出的三个缺口。所有生产改动前均先运行新增测试并观察正确 RED；未调用真实模型。

### F1. Per-conclusion logical evidence mapping and provenance

- 根因：`BlueprintOrchestrator._build_blueprint()` 仅按绝对 range 去重，导致不同 agent 在同一范围上的独立 provenance 丢失；`BlueprintService._publish_reference_blueprint()` 又把所有 observed/uncertain dimension 强制改写为第一条 DB evidence id。
- 修复文件：`creative_claw/blueprint_orchestrator.py`、`creative_claw/blueprint_service.py`、`tests/test_blueprint_service.py`。
- 处理：每个 run-local logical evidence id 先映射为包含 run identity 的 blueprint logical id；相同范围不再折叠不同 agent provenance。发布时建立显式 logical id → DB evidence id 映射，并逐结论重写 refs。observed/uncertain 引用缺失或引用其他 run 未声明 evidence 时以 `schema_failed` 阻止任务/发布。
- RED 断言：同范围三 agent 的 `len(set(refs.values())) == 1`（预期 3）；缺证据 job 实际为 `completed`（预期 `blocked`）。
- RED/GREEN 命令：
  `.\.venv\Scripts\python.exe -m unittest tests.test_blueprint_service.BlueprintServiceTests.test_reference_publication_preserves_per_conclusion_evidence_provenance tests.test_blueprint_service.BlueprintServiceTests.test_reference_publication_rejects_observed_dimension_with_missing_evidence -v`
- GREEN：两条用例通过；三条 evidence row 保留相同 `(start,end)`，但 DB ids 与 `agent_run_id` 各自独立。

### F2. Per-agent machine-readable contracts and unified payload validation

- 根因：`OpenAICompatibleBlueprintAgent` 的 system prompt 只有通用 schema 名称与维度数量；orchestrator 只浅验三个 agent，setting/migration/generation agent 没有统一的消费前验证。
- 修复文件：`creative_claw/blueprint_agents.py`、`creative_claw/blueprint_orchestrator.py`、`creative_claw/blueprint_service.py`、`tests/test_blueprint_agents.py`。
- 处理：新增 `agent_output_contract()`、`build_agent_system_prompt()`、`validate_agent_payload()`。所有 reference/setting/migration/generation agent 都获得对应 JSON contract；reference contract 明确 23 dimensions 三态与 batch-local evidence ranges，其余 contract 覆盖 nodes、edges、interpretations/conflicts、structured setting、mappings、target nodes、unit plan、draft、continuity 与 safety verdict。orchestrator 与 service 在持久化/消费前统一验证；service 非 reference 非法 payload 写 `schema_failed` run 并抛出带 agent 名的明确 `ValueError`。
- RED 断言：三个 contract/validator API 均不存在；invalid setting payload 未记录 `schema_failed`。
- RED/GREEN 命令：
  `.\.venv\Scripts\python.exe -m unittest tests.test_blueprint_agents.BlueprintAgentContractTests -v`
- GREEN：3/3；测试只解析纯 prompt builder 与本地 fake agent，不执行 `requests.post`。
- 兼容性 RED/GREEN：首次全 focused 因 deterministic hierarchy fixture 缺少 contract 所需 nodes 产生 5 failures / 16 errors；补齐最小 work node 后 orchestrator 8/8、service 22/22，fresh focused 46/46。

### F3. Closed mechanism-class DTO and dual-boundary leak prevention

- 根因：`_abstract_reference()` 直接复制任意 `value.mechanism_class` 自由字符串，reference sentinel 可进入 mapping/target agent context，并可能随 target blueprint 到达 generation request。
- 修复文件：`creative_claw/blueprint_service.py`、`tests/test_blueprint_service.py`。
- 处理：narrative function、causality、emotion K-line 分别使用封闭允许枚举；未知/缺失值映射为不含原文、不含 hash 的 `generic:<dimension>` neutral class。
- RED 断言：`MECHANISM_CLASS_REFERENCE_SENTINEL_R4Q` 明确出现在 `mechanism_mapping_agent` 与 `target_blueprint_agent` context。
- RED/GREEN 命令：
  `.\.venv\Scripts\python.exe -m unittest tests.test_blueprint_service.BlueprintServiceTests.test_unknown_reference_mechanism_class_never_reaches_migration_or_generation_agents -v`
- GREEN：sentinel 不在 migration inputs 或后续任一 generation agent input；migration 接收 `generic:narrative_function`。

### Follow-up verification

```text
Initial RED: 6/6 failed for the expected missing behavior (17.639s)
Targeted GREEN: 6/6 passed (20.793s)
Compatibility modules: orchestrator 8/8; service 22/22
Fresh blueprint focused: 46/46 passed (238.064s)
Node: 16/16 passed
JS syntax: context-state.js / blueprint-state.js / app.js all exit 0
Python py_compile for changed production/tests: exit 0
```

## Remaining concerns

- 未运行真实模型验证（按授权要求保持 skipped）；生产模型供应商的 JSON 稳定性仍需在显式授权后单独验收。
- 首版执行器仍是本地单用户 SQLite/线程池，不是云队列或多人协作系统。
- interpretation/mapping 的同毫秒顺序使用 SQLite `rowid` 作为插入序 tie-breaker；这符合当前固定 SQLite 技术栈，若未来迁移数据库应增加显式 ordinal 列。
- E2E 控制台仅有浏览器请求 `/favicon.ico` 的非功能性 404；`page_errors=[]`，所有业务断言通过。
- 无 Git 历史或 diff 可供审阅；本报告依赖测试证据与逐文件检查，目录仍未初始化 Git。

## Second scoped residual review（2026-07-29）

本节记录 Halley 第二次 scoped re-review 的六组 residual。每项生产改动前均先运行新增回归并观察正确 RED；所有 agent 测试使用 deterministic/fake agent，`CREATIVE_CLAW_REAL_LLM_TEST=0`，未调用真实模型。

### R1. Migration tail-state cancellation/pause

- 根因：migration 只在最终 publication transaction 检查 `desired_state`；mapping 或 target agent 返回后发生 cancel/pause 时，流程仍调用下一个 agent或进入发布，最后抛出 `migration job is not running`。
- 修复：`creative_claw/blueprint_service.py` 在 mapping agent 后、target agent 后分别重新读取 job；非 running 立即持久化 `cancelled`/`paused` 并返回，target artifact/version/node/mapping 均不创建。
- RED：mapping 后 cancel 仍调用 target；target 后 pause 仍进入 publication，两条均以 `ValueError` 退出。
- GREEN：`test_migration_cancelled_by_mapping_agent_never_calls_target_or_publishes`、`test_migration_paused_by_target_agent_never_publishes` 2/2。

### R2. Style fingerprint separation and closed safety verdict

- 根因：reference feature extraction 已产生 `style_fingerprints`，但 similarity call 未传递；safety finding 接受任意 dict，并且 candidate 在 safety verdict 校验前已落库。
- 修复：`creative_claw/blueprint_similarity.py` 独立计算 `style_fingerprint_hit_count`，输出 candidate/reference 双侧位置；`creative_claw/blueprint_agents.py` 将 verdict、finding、range、metric、remediation 全部改为封闭 contract（`additionalProperties: false`）；`creative_claw/blueprint_service.py` 在 verdict 验证完成后才创建 candidate/assessment。
- RED：`assess_similarity(..., style_fingerprints=...)` 抛 unexpected keyword；带 reference passage 的 `detail` finding 被接受且 candidate 被持久化。
- GREEN：style fingerprint 定位、原 typed escalation 兼容、恶意 verdict `schema_failed` 且 candidate/assessment 零新增。

### R3. Hierarchy enrichment, global tree validation and conflict offsets

- 根因：merge 对同 stable key 使用 `setdefault`，segmentation 永久压住 hierarchy；conflict indexes 未按全局 interpretations 数量重基；聚合后没有全局树校验。
- 修复：`creative_claw/blueprint_orchestrator.py` 使用 agent priority 合并同 key，hierarchy 可富化 title/summary/parent/dimensions，同时保留 segmentation locator；每个 conflict run 加 interpretation offset；发布前验证唯一 work root、允许的 work→volume?→chapter→scene→beat 父子关系、orphan、cycle 与最终 root reachability。非法树将 job 标为 `blocked / hierarchy_invalid`。deterministic hierarchy 现在与 segmentation 使用同 key，并生成真实 beat。
- RED：hierarchy title 仍是 segmentation title；多批 indexes 重复从 0 开始；orphan/cycle/非法父类型均 `completed`。
- GREEN：多批 enrichment/locator/parent/beat/offset 用例与三类非法树用例 4/4；orchestrator fresh 12/12。
- 兼容修复：deterministic placeholder beat 没有可比较的 role/event/outcome，曾把结构相似分母从 10 稀释为 11；现仅将具有至少一个可比较字段的 persisted reference beat 纳入分母。对应 service regression targeted GREEN。

### R4. Durable reference source/job idempotency

- 根因：source artifact/version 在 job 之前分别提交；job create 失败留下 stranded source，重试会再建 source/version。跨 service instance 绕过进程锁后，document path 的查后建发生唯一键竞态。
- 修复：`creative_claw/blueprint_service.py` 用 SQLite `BEGIN IMMEDIATE` 在同一 durable critical section 查后建 reference source/version，并按 project/source hash/rights 复用 stranded source；document path 唯一键/lock 竞态做有限本地重试。`BlueprintRepository.create_job()` 的幂等查后建也使用 `BEGIN IMMEDIATE`。进程锁不再是正确性的唯一来源。
- RED：job failure retry 得到 1 document / 2 source / 2 version / 1 job；双 service 绕锁并发触发 `UNIQUE constraint failed: documents.project_id, documents.path`。
- GREEN：failure retry、双 service 绕锁并发、原同 service 并发 3/3，均为 1 document/source/version/job 且返回同一 job。

### R5. Genuine schema-v4 fixture

- 根因：旧测试先执行 v5 `Database.initialize()`，再 drop blueprint tables 并把 `user_version` 改为 4，不是真实升级起点。
- 修复：新增 `tests/fixtures/schema_v4.sql`，仅定义 v4 workflow/artifact/OHLC/ledger 所需表并设置 `PRAGMA user_version = 4`；测试先写入真实 project/workflow/artifact/version/OHLC/ledger rows，确认 blueprint tables 不存在，再执行 v5 initialize 并逐项核对保真与 ledger hash chain。
- GREEN：`test_v4_upgrade_preserves_workflow_artifacts_ohlc_and_ledger` 1/1；测试源码不再 initialize-v5-then-drop。

### R6. Failed attempt persistence for non-schema exceptions

- 根因：migration/target-setting/generation runner 只捕获 `ValueError` 为 `schema_failed`；timeout 等异常直接冒泡，不创建 run，下一次 attempt 也无法审计。
- 修复：`ValueError`/JSON contract failure 继续记录 `schema_failed`；其他 `Exception` 记录 `retryable_failed` 后原样抛出。每次 attempt 使用独立 idempotency key，重试只复用 `completed`。
- RED：generation 与 target-setting 首次 timeout 后数据库只有第二次 `completed` run。
- GREEN：两条均得到 `retryable_failed → completed`，两个 attempt keys 不同。

### Second residual fresh verification

单条 combined focused command 因当前 runner 的 360 秒进程上限被终止且没有返回断言输出，因此按模块 fresh 拆跑并读取完整结果；不是把超时计为通过：

```text
agents + similarity + schema + API: 19/19, OK (26.452s)
orchestrator: 12/12, OK (84.126s)
service: 27/27, OK (263.479s)
Focused total: 58/58, OK
Node: 16/16, pass
JS syntax: context-state.js / blueprint-state.js / app.js, all exit 0
Python compileall: exit 0
Phase 1 context E2E: passed=true, exit 0
Phase 2.5 blueprint E2E: passed=true, exit 0
Credential-shaped literal scan: NO_CREDENTIAL_SHAPED_LITERALS
Blueprint report privacy scan: REPORT_HASH_ONLY_AND_DRAFT_ALLOWLIST_OK
Browser storage scan: NO_REFERENCE_OR_KEY_BROWSER_STORAGE
```

第二次 blueprint E2E report 只保存 `reference_hash` / `safe_draft_hash`；两次 draft request 均仅含 `artifact_id`、`target_blueprint_id`、`unit_id`。两份 E2E 的 `page_errors=[]`；控制台仍只有 `/favicon.ico` 非功能性 404。

## Unified batch-key and staged-synthesis repair (2026-07-29)

This section records the final Phase 2.5 repair wave. All agent runs in this wave used deterministic or local fake agents. No paid API or real-model request was made.

### Root causes and implemented behavior

1. Batch-local agent keys had no canonical namespace. Repeated `chapter:1`, `scene:1`, and `beat:1` declarations from three batches collapsed into one node set. The orchestrator now consumes explicit `key_scope: batch | global`, defaults historical non-work nodes to `batch` and `work` to `global`, and canonicalizes local keys as `batch:<ordinal>:<raw-key>`.
2. Canonical remapping now covers nodes, parent keys, edges, interpretations, conflict groups, reference publication, and migration inputs. Mixed scope or type declarations for the same raw key fail as `hierarchy_invalid`; ambiguous job-global references to repeated raw batch keys also fail instead of guessing.
3. Conflicting parents for the same explicitly global node across hierarchy runs were previously resolved by last-write-wins. The pre-merge scan now compares canonical parent declarations from hierarchy runs and rejects conflicts. Segmentation-to-hierarchy enrichment remains legal, and explicitly global shared nodes still merge once.
4. The legal hierarchy validator now supports `work -> volume|phase`, `volume|phase -> chapter|episode`, the shortened `work -> chapter|episode` branch, `chapter|episode -> scene`, and `scene -> beat`.
5. The old DAG ran hierarchy and conflict once per batch. Batch processing now runs the specialist DAG plus chapter synthesis only. After every batch completes, job-global barriers run in order: `volume -> work -> conflict`. A partial `max_batches` run never starts global synthesis.
6. Each global stage has its own prompt version, idempotency key, attempt sequence, completed-only reuse, and desired-state checks before and after execution. Pause/cancel prevents later stages; retry creates a fresh attempt without repeating completed earlier stages.
7. Global-stage context is limited to typed canonical node summaries, lower-level summaries, and evidence metadata. A recursive test excludes `text`, `quote`, `rare_phrases`, `style_fingerprints`, and `raw_agent_response` at every nesting depth.

### RED evidence

Canonical batch-key tests:

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_blueprint_orchestrator.BlueprintOrchestratorTests.test_multibatch_reused_local_keys_are_namespaced_and_all_references_remapped tests.test_blueprint_service.BlueprintServiceTests.test_published_and_migration_inputs_use_canonical_multibatch_keys -v
```

- Orchestrator RED: expected three chapters/scenes/beats, actual one each.
- Service RED: canonical `batch:<ordinal>:chapter:1` keys were absent from published and migration inputs.

Phase/episode tests initially returned `blocked` where `completed` was required:

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_blueprint_orchestrator.BlueprintOrchestratorTests.test_global_hierarchy_accepts_phase_episode_branch tests.test_blueprint_orchestrator.BlueprintOrchestratorTests.test_global_hierarchy_accepts_short_work_episode_branch -v
```

Staged synthesis tests were each observed RED before implementation:

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_blueprint_orchestrator.BlueprintOrchestratorTests.test_long_job_runs_chapter_volume_work_then_final_conflict_with_safe_global_contexts tests.test_blueprint_orchestrator.BlueprintOrchestratorTests.test_partial_batches_never_start_global_synthesis_or_conflict tests.test_blueprint_orchestrator.BlueprintOrchestratorTests.test_pause_after_volume_stage_prevents_work_and_resumes_without_repeating_volume tests.test_blueprint_orchestrator.BlueprintOrchestratorTests.test_retryable_work_stage_uses_new_attempt_without_repeating_completed_volume -v
```

- Stage context had no stage identifier and no volume/work barriers.
- Partial processing still invoked the conflict agent.
- Pause after volume incorrectly completed the job.
- A retryable work-stage failure incorrectly completed instead of blocking for a new attempt.

The global-parent conflict regression was added and observed RED independently:

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_blueprint_orchestrator.BlueprintOrchestratorTests.test_global_node_parent_conflict_across_hierarchy_runs_is_rejected -v
```

Observed failure: actual job status `completed`, expected `blocked`. The minimal production fix added canonical parent-set validation; the focused parent-conflict plus legal shared-global regression then passed 2/2.

The first compatibility run exposed one production classification regression and four obsolete test assumptions:

```text
agents + orchestrator: 24 tests; 6 failures, 1 error
```

Three invalid-hierarchy cases were incorrectly categorized as `agent_failed` because global-stage blueprint construction shared the agent-call exception block. Blueprint construction now has a separate `ValueError -> hierarchy_invalid` boundary. The remaining tests were updated for canonical local keys, three hierarchy stages, final job-global conflict, and explicit global shared nodes. Service fixtures were also made stage-aware so global stages do not expect reference text or batch-local evidence.

### GREEN and final verification evidence

Targeted results:

```text
canonical orchestrator + publication/migration: 2/2, OK (31.624s)
phase/episode plus invalid hierarchy compatibility: 5/5, OK (25.152s)
staged synthesis/pause/retry/partial: 4/4, OK (49.060s)
scope/type/ambiguous-reference/cancel boundaries: 4/4 GREEN; parent conflict RED then GREEN
parent conflict plus legal explicit-global merge: 2/2, OK (40.816s)
```

Fresh focused verification, split by module to avoid treating runner timeouts as success:

```text
blueprint_agents + blueprint_orchestrator: 29/29, OK (217.077s)
blueprint_service: 28/28, OK (219.292s)
blueprint_similarity: 6/6, OK (0.061s)
blueprint_schema: 5/5, OK (11.354s)
blueprint_api: 3/3, OK (19.953s)
Focused Python total: 71/71, OK
Node tests: 16/16, pass (281.7071ms)
JS syntax: context-state.js / blueprint-state.js / app.js, all exit 0
Python compileall: exit 0
```

Commands:

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_blueprint_agents tests.test_blueprint_orchestrator -v
.\.venv\Scripts\python.exe -m unittest tests.test_blueprint_service -v
.\.venv\Scripts\python.exe -m unittest tests.test_blueprint_similarity -v
.\.venv\Scripts\python.exe -m unittest tests.test_blueprint_schema -v
.\.venv\Scripts\python.exe -m unittest tests.test_blueprint_api -v
node --test tests/js/*.test.cjs
node --check creative_claw/web/context-state.js
node --check creative_claw/web/blueprint-state.js
node --check creative_claw/web/app.js
.\.venv\Scripts\python.exe -m compileall -q creative_claw tests scripts
```

### Residual risks

- Historical payloads without `key_scope` intentionally default non-work nodes to batch scope for safety. A genuinely shared cross-batch volume, phase, chapter, episode, scene, or beat must explicitly declare `global`.
- The staged context firewall enforces structural allowlists and removes known raw/reference fields. Semantic quality of model-produced typed summaries still requires explicit real-model acceptance testing.
- Real-model validation remains skipped by authorization; production-provider JSON stability has not been exercised in this wave.
- No Git repository was initialized and no Git command or commit was used.

## Final Important fix: evidence-live global synthesis (2026-07-29)

### Root cause

Global volume/work synthesis was structurally present but evidence-dead:

- `_run_synthesis_agent()` validated global output with `batch_length=0` and the same rule used for batch extraction: every dimension reference had to point to evidence created in the current response.
- Reusing an ID from `context.evidence_metadata` therefore produced `schema_failed`; returning a new range produced `evidence_invalid` because no global stage owns source text or a local range.
- `_synthesis_context()` removed dimension `value` and omitted top-level blueprint dimensions, so a global agent did not receive a closed typed lower-level DTO.
- `_build_blueprint()` ignored global hierarchy `data["dimensions"]`, node dimensions, and evidence references. A real agent could only return all `not_observed` or block the job.

### TDD evidence

Added fake-agent integration coverage:

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_blueprint_orchestrator.BlueprintOrchestratorTests.test_global_hierarchy_reuses_existing_evidence_and_merges_typed_dimensions tests.test_blueprint_orchestrator.BlueprintOrchestratorTests.test_global_hierarchy_unknown_existing_evidence_ref_is_schema_failed tests.test_blueprint_orchestrator.BlueprintOrchestratorTests.test_global_hierarchy_cannot_create_new_evidence_range -v
```

Initial result:

```text
3 tests: 1 failed, 2 passed
legal existing-evidence synthesis: RED, job blocked at volume (expected completed)
unknown existing evidence id: already blocked with schema_failed
fabricated global evidence range: already blocked with evidence_invalid
```

The two already-green cases are negative security constraints; the positive integration case was the required missing-behavior RED.

### Minimal implementation

- `validate_agent_payload()` now accepts an explicit `existing_evidence_refs` allowlist. Batch calls pass no allowlist, so their evidence rules are unchanged.
- Global synthesis derives the allowlist only from `context.evidence_metadata[].id`. Its response evidence array must be empty. Unknown external IDs remain `schema_failed`; any new range remains `evidence_invalid`.
- The global prompt explicitly instructs agents to return no new evidence and cite only supplied metadata IDs.
- `_synthesis_context()` now includes closed 23-dimension DTOs (`state`, sanitized `value`, `confidence`, `evidence_refs`) for canonical/lower nodes and a top-level `blueprint_dimensions` DTO.
- Recursive sanitization removes `text`, `quote`, `rare_phrases`, `style_fingerprints`, `raw_response`, and `raw_agent_response` keys before global context construction. The draft firewall was not changed.
- `_build_blueprint()` merges global hierarchy top-level and node dimensions with explicit stage priority (`work=4`, `volume=3`, batch/lower stages below them). Informative work conclusions deterministically override volume conclusions; `not_observed` does not erase an informative lower-stage result.
- Global synthesis reuses canonical existing evidence IDs and never appends an evidence row. The integration test verifies the final evidence ID list is identical to the pre-global metadata list, remains unique, and every retained global ref resolves to it.

### GREEN and fresh verification

```text
global evidence integration/security: 3/3, OK (35.666s)
blueprint_agents + blueprint_orchestrator: 32/32, OK (244.559s)
blueprint_service: 28/28, OK (223.906s)
Python compileall: exit 0
```

The positive integration test additionally verifies:

- volume and work can emit observed/uncertain dimensions citing lower evidence;
- final top-level and global node dimensions preserve value, confidence, and original refs;
- work has deterministic priority over volume;
- original batch evidence provenance is neither duplicated nor dropped;
- global context contains full typed lower-node and top-level dimensions while recursively excluding forbidden reference/raw-response keys.

No real model or paid API was called, and no Git operation was used.

## Evidence-live fix loop round 2: recursive reference redaction

### New Important root cause

The evidence-live context added dimension `value`, but the first sanitizer only compared exact lowercase dictionary keys. It did not inspect arbitrary strings, case/separator variants, node title/summary, or source-locator content. A batch agent could therefore echo a reference sentence through `value`, `mechanism_class`, `Quote`, `RAW-Agent-Response`, `Style_Fingerprints`, `title`, nested `summary`, or `source_locator.text`, and that content would be sent to volume/work/conflict tasks.

### RED integration evidence

The new test places `GLOBAL_CONTEXT_REFERENCE_SENTINEL_R8Q4` in the actual job reference source, then injects it through every path above while also supplying safe abstract and scalar fields.

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_blueprint_orchestrator.BlueprintOrchestratorTests.test_global_context_recursively_redacts_reference_derived_payloads -v
```

Initial result:

```text
1/1 failed as expected (12.609s)
volume context contained the source sentinel in direct value, mechanism_class, title,
nested summary, source_locator.text, Quote, RAW-Agent-Response, and Style_Fingerprints
```

### Minimal implementation

- `_synthesis_context()` now receives `reference_text` only as a local sanitizer input. The caller passes the current job input text; it is never added to the returned task context or global run result.
- Forbidden dictionary keys are compared after `casefold()` plus normalization of every non-alphanumeric separator. `Quote`, `RAW-Agent-Response`, and `Style_Fingerprints` therefore map to their closed forbidden forms.
- Dimension values, node title, summary, and source locator all use the same recursive sanitizer.
- A string is replaced with the constant non-hash marker `[redacted-reference]` when it is a source substring with at least two Chinese characters, or when its normalized Latin/digit form has length at least eight and occurs in normalized source text.
- Safe abstract strings that are not in the source, including `costly_choice`, remain available. Numbers, booleans, state, confidence, and evidence references are preserved unchanged.
- Stable/canonical identifiers and evidence allowlisting were not relaxed. The draft firewall was not changed.

The integration test also proves that the raw reference text, sentinel, and source hash do not occur in any volume/work/conflict task context or visible persisted global-run payload. Global `input_hash` remains only the hash of the already-sanitized task context and is not equal to the source hash.

### GREEN and fresh verification

```text
recursive redaction + evidence-live + original staged firewall: 3/3, OK (31.836s)
blueprint_agents + blueprint_orchestrator: 33/33, OK (257.524s)
blueprint_service: 28/28, OK (235.096s)
Python compileall: exit 0
```

No Git command, real-model call, or paid API request was used.

## Evidence-live fix loop round 3: wrapped-fragment n-grams

### Reproduced bypasses

Round 2 compared each complete scalar against the source. It missed wrapped fragments such as `abstract: EN_SENTINEL_R9Q4 suffix` and `摘要：星河密令（抽象包装）`. The canonical forbidden-key set also omitted singular/plural and reference/source/passage/raw-model variants.

The new fake-agent integration test places both sentinels in the actual job source, wraps them with arbitrary prefixes/suffixes in value, mechanism class, title, summary, and locator fields, and injects case/separator variants including:

```text
Reference-Text / SOURCE_TEXTS
Rare-Phrase / RARE_PHRASES
Style-Fingerprint / STYLE_FINGERPRINTS
Passage / PASSAGES
Reference_Passage / SOURCE-PASSAGES
RAW-MODEL-RESPONSE / Raw_Model_Responses
Fingerprint / FINGERPRINTS
```

RED command:

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_blueprint_orchestrator.BlueprintOrchestratorTests.test_global_context_redacts_wrapped_source_ngrams_and_forbidden_key_variants -v
```

Observed RED: 1/1 failed because the wrapped English/Chinese sentinels and the new canonical key variants were present in volume context.

### Minimal implementation

- The sanitizer precomputes every continuous Chinese source 2-gram and normalized Latin/digit source 8-gram.
- Candidate strings are normalized the same way; any n-gram intersection replaces the entire scalar with the fixed `[redacted-reference]` marker. This detects source fragments embedded inside arbitrary surrounding text rather than only whole-scalar equality.
- The normalized forbidden-key set now closes singular/plural forms for reference/source text, rare phrases, style fingerprints, passages, reference/source passages, raw responses, raw agent/model responses, and generic fingerprints, including case and non-alphanumeric separator variants.
- Safe `costly_choice`, numbers, booleans, state, confidence, and evidence refs remain intact. Existing-evidence allowlisting and batch contracts are unchanged.

### GREEN and fresh verification

```text
wrapped n-gram + round-2 redaction + evidence-live compatibility: 3/3, OK (56.048s)
blueprint_agents + blueprint_orchestrator: 34/34, OK (250.624s)
blueprint_service: 28/28, OK (233.261s)
Python compileall: exit 0
```

No source hash or source fragment is returned in global context; no Git command, real-model call, or paid API request was used.

## Evidence-live fix loop round 4: split-CJK and CamelCase key closure

### Root cause

Round 3 derived Chinese 2-grams independently from each continuous CJK run. If punctuation, whitespace, or emoji was inserted between every source character, each candidate run had length one and produced no 2-grams, allowing wrapped source content to reach volume/work/conflict context. The dictionary-key normalizer also applied `casefold()` before word-boundary detection, so CamelCase keys such as `ReferenceText` and acronym-boundary keys such as `RAWAgentResponse` collapsed to `referencetext` / `rawagentresponse` instead of the forbidden canonical forms.

### RED evidence

The new deterministic fake-agent integration test places a continuous Chinese sentinel in the actual reference source, then echoes it through title, summary, locator, mechanism, and nested dimension values with punctuation, whitespace, and emoji between every CJK character. It also recursively injects `ReferenceText`, `SourceText`, `RarePhrase`, `StyleFingerprint`, `ReferencePassage`, `SourcePassage`, `RawAgentResponse`, `RawModelResponse`, `RAWAgentResponse`, and `RAWModelResponse`.

```powershell
$env:CREATIVE_CLAW_REAL_LLM_TEST='0'
.\.venv\Scripts\python.exe -m unittest tests.test_blueprint_orchestrator.BlueprintOrchestratorTests.test_global_context_redacts_split_cjk_and_camelcase_forbidden_keys tests.test_blueprint_orchestrator.BlueprintOrchestratorTests.test_synthesis_context_handles_empty_and_short_references -v
```

Initial result: 2 tests ran, the empty/short-reference boundary passed, and the integration test failed because forbidden CamelCase/acronym keys remained in the recursive canonical key set. Before any production edit, the string assertion was strengthened to recursively inspect real scalar values after removing non-CJK characters; the focused rerun then failed at the expected source-fragment assertion because the split candidate reconstructed the continuous Chinese sentinel.

### Minimal implementation

- Reference and candidate strings now remove every non-CJK character and concatenate the remaining CJK sequence before 2-gram construction. Empty and one-character references still produce an empty gram set without raising.
- Key normalization now detects acronym-to-capitalized and lower/digit-to-uppercase boundaries before `casefold()` and separator normalization. The existing forbidden canonical set therefore removes the listed CamelCase and `RAW...` variants recursively.
- No global-evidence or batch-evidence validation code changed. A characterization test explicitly verifies that a batch hierarchy conclusion cannot cite evidence from a prior response when the current response supplies a different evidence ID.
- The integration test verifies all three global contexts omit the reconstructed CJK sentinel and forbidden keys while preserving useful observed causality synthesis, `costly_choice`, integers, booleans, state, confidence, and evidence refs.

### GREEN and fresh verification

```text
new split-CJK/CamelCase + empty/short boundaries: 2/2, OK (13.337s)
sanitizer/evidence/batch-boundary focused compatibility: 8/8, OK (71.269s)
batch current-response evidence characterization: 1/1, OK (1.823s)
blueprint_agents + blueprint_orchestrator final fresh: 37/37, OK (277.923s), exit 0
blueprint_service fresh: 28/28, OK (367.204s), exit 0
Python compileall final fresh: exit 0
```

Fresh commands:

```powershell
$env:CREATIVE_CLAW_REAL_LLM_TEST='0'
.\.venv\Scripts\python.exe -m unittest tests.test_blueprint_agents tests.test_blueprint_orchestrator -v
.\.venv\Scripts\python.exe -m unittest tests.test_blueprint_service -v
.\.venv\Scripts\python.exe -m compileall -q creative_claw tests scripts
```

All tests used deterministic/local fake agents with `CREATIVE_CLAW_REAL_LLM_TEST=0`. No Git repository was initialized, no Git command was run, and no real-model or paid API request was made.

## Main-agent final full verification (2026-07-29)

After the round-4 scoped reviewer returned `ADDRESSED / READY`, the primary process independently reran the complete project verification rather than relying on implementer reports.

```text
Python unittest discovery: 117 total, 116 passed, 1 paid real-model test skipped, exit 0 (585.507s)
Python compileall: exit 0
Node tests: 16/16 passed
JavaScript syntax: context-state.js / blueprint-state.js / app.js, all exit 0
Phase 1 context E2E: passed=true, page_errors=[], service_stopped=true
Phase 2.5 blueprint E2E: passed=true, page_errors=[], service_stopped=true
Blueprint E2E draft requests: 2/2 contained only artifact_id, target_blueprint_id, unit_id
Credential-shaped literal scan: NO_CREDENTIAL_SHAPED_LITERALS
E2E report/privacy scan: REPORT_HASH_ONLY_AND_DRAFT_ALLOWLIST_OK
Browser storage scan: NO_REFERENCE_OR_KEY_BROWSER_STORAGE
```

The blueprint E2E report contains only `reference_hash` and `safe_draft_hash`, not the complete reference or accepted draft. Browser storage calls are limited to the active project id and canvas positions. Both E2E reports have only the known non-functional `/favicon.ico` 404 in console output and no page errors.

`CREATIVE_CLAW_REAL_LLM_TEST=0` was set explicitly. No real-model request or paid API call occurred. The directory is still not a Git repository; no initialization or commit was performed.
