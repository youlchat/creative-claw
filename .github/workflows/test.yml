name: test

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  python:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.11", "3.12"]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip
      - run: python -m pip install -e .
      - run: python -m compileall -q creative_claw examples tests
      - run: python -m unittest discover -s tests -v
      - name: Verify repository-contained bootstrap
        run: python examples/bootstrap_demo.py --db /tmp/creative-claw.db --project-root /tmp/creative-claw-project
